tar -xvf deepstream_15_release.tar.gz -C /usr/local/
sudo apt-get install build-essential
sudo apt-get install libgl1-mesa-dev
sudo apt-get install libglu1-mesa-dev
sudo apt-get install freeglut3-dev
sudo apt-get install libopencv-dev python-opencv

(Ref https://devtalk.nvidia.com/default/topic/1035597/compiling-nvdecinfer_detection-fail/?offset=5)

#### modify PATH

# add deepstream path to /etc/profile
export LD_LIBRARY_PATH=/usr/local/deepstream/lib:$LD_LIBRARY_PATH


#TesorRt (dev package version 4.0)
dpkg -i nv-tensorrt-repo-ubuntu1604-cuda9.0-ga-trt4.0.1.6-20180612_1-1_amd64.deb
apt-get update
apt-get install tensorrt

# copy Video_Codec_SDK_8.0.14.zip to /usr/local and unzip using belo command

 unzip Video_Codec_SDK_8.0.14.zip

 cd Video_Codec_SDK_8.0.14/Samples

#make

# you mae get -lXmu error
# go to Video_Codec_SDK_8.0.14/Samples/NvDecodeGL
# edit make file by removig -lXmu from LDFLAGS

# go to /usr/local/deepstream/samples   and compile using make file
  
make

#sudo apt-get install libxmuu-dev

# to install nccl 

sudo apt install libnccl2=2.3.7-1+cuda9.0 libnccl-dev=2.3.7-1+cuda9.0


###  to test deep steam and NCCL #########

#deep steam 

go to /usr/local/deepstream/samples/decPerf and give below command
./run.sh


# NCCL

git clone https://github.com/NVIDIA/nccl-tests.git
cd nccl-tests
make MPI=1 MPI_HOME=/usr/lib/openmpi
./build/all_reduce_perf -b 8 -e 128M -f 2 -g 2
mpirun -np 2 ./build/all_reduce_perf -b 8 -e 128M -f 2 -g 1 -c 0










